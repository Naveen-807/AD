# Exercise 09 Flutter Expense Manager

## Run

1. Install Flutter SDK.
2. From this folder run:

```bash
flutter create .
flutter pub get
flutter run
```

`flutter create .` generates missing platform folders and keeps `lib/main.dart`.
