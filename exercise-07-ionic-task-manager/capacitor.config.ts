import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'exercise-07-ionic-task-manager',
  webDir: 'www'
};

export default config;
